import fs from 'fs';

export default abstract class luaLoader {
    static loadFilesInDirectory(directory: directory) {
        let files: luaFile[] = [];
        
        let directoryFiles: File[] = []
    }
}